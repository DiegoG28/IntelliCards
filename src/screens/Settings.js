import { Text } from 'react-native';
import Layout from '@components/Layout';

const Settings = () => {
   return (
      <Layout>
         <Text>Setting</Text>
      </Layout>
   );
};

export default Settings;
