import Onboard from '@components/Onboard';

const OnboardingTwo = () => {
   return (
      <Onboard title="Autocompletado de información sobre músculos">
         Crea tarjetas escribiendo únicamente el músculo que quieras estudiar.
         Nosotros haremos el resto por ti. 2
      </Onboard>
   );
};

export default OnboardingTwo;
